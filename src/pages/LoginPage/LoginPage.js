import { Wrapper, Container } from './LoginPage.styled';
// import LoginForm from '../LoginForm';

const LoginPage = () => {
  return (
    <Container>
      <Wrapper>LoginForm</Wrapper>
    </Container>
  );
};

export default LoginPage;
